interface:
  display_name: "报销读取"
  short_description: "读取报销材料，匹配附件并生成OA清单"
  default_prompt: "Use $reimbursement-reader to read reimbursement files, calculate eligible travel meal allowance, match attachments, and generate an OA checklist."
